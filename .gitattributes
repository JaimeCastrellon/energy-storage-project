# Normalize line endings to LF in the repo for all text files,
# regardless of what OS checks them out. This stops the CRLF/LF
# warnings and keeps diffs clean across Windows/Mac/Linux.
* text=auto eol=lf

# Windows-specific scripts should keep CRLF if you ever add any
*.bat text eol=crlf
